#include <stdlib.h>
#include <stdio.h>
//nochmal ran
int main()
{

    float a[10];
    float summe;
    for (int i = 0; i < 10; i++)
    {
        printf("Zahl eingeben zwischen 1-15:");
        scanf("%d", &a[i]);
        summe = summe + noten[i];
    }
    float b = 0;
    for (int i = 0; i < 9; i++)
    {
        int z = a[i];
        b + z;
    }

    printf("Ausgabe: %d", b/10);

    return 0;
}
